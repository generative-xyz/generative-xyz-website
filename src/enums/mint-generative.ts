export enum MintGenerativeStep {
  UPLOAD_PROJECT = 'upload-project',
  PROJECT_DETAIL = 'project-detail',
  SET_PRICE = 'set-price',
}

export enum PreviewDisplayMode {
  Animation = 'Animation',
  Thumbnail = 'Thumbnail',
}
